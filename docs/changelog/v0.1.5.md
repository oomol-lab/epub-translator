# Release v0.1.5

This hotfix release resolves a critical crash when translating EPUB files with nested inline elements.

## 🐛 Bug Fixes

### Fixed Crash with Nested Inline Elements (#107)

Fixed a `ValueError: Element not found in parent` crash when processing platform structures containing nested inline elements (e.g., `<math>` inside `<span>`).

Related PR: https://github.com/oomol-lab/epub-translator/pull/109

## 📦 Installation

```bash
pip install epub-translator==0.1.5
```

Or upgrade:

```bash
pip install --upgrade epub-translator
```

## Migration from v0.1.4

Fully backward compatible with v0.1.4. No code changes required.
